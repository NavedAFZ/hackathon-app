<%@c1=Challenge.new%>
<%= form_with(model: @c1, local: true) do |form| %>
  <% if @c1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c1.errors.count, "error") %> prohibited this challenge from being saved:</h2>

      <ul>
        <% @c1.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col"><%= form.label :title %></th>
      <th scope="col"><%= form.label :description %></th>
      <th scope="col"> <%= form.label :tags %> </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= form.text_field :title %></td>
      <td><%= form.text_field :description %></td>
      <td><%= form.text_field :tags %></td>
    </tr>
    
  </tbody>
</table>
    <div class="field">
    <%= form.number_field :user_id ,id: :challenge_user_id, value: current_user.id, type: :hidden%>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
