<%@c1=Challenge.new%>
<%= form_with(model: @c1, local: true) do |form| %>
  <% if @c1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c1.errors.count, "error") %> prohibited this challenge from being saved:</h2>

      <ul>
        <% @c1.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :tags %>
    <%= form.text_field :tags %>
  </div>
    <div class="field">
    <%= form.number_field :user_id ,id: :challenge_user_id, value: current_user.id, type: :hidden%>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
